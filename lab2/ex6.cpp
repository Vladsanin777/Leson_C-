#include <iostream>
#include <cmath>

using namespace std;

int main(){
	double x, y, z;
	cin>>x>>y>>z;
	cout<<"a = "<<pow(tan(cos(sqrt(3)) + sin(y)), 2)<<endl<<"b = "<<pow(y, 2) / (pow(x, 3) + sqrt(pow(z, 3) + 1))<<endl;
}
