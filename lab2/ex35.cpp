#include <iostream>
#include <cmath>

using namespace std;

int main(){
	double x, y, z;
	cin>>x>>y>>z;
	cout<<"a = "<<sin(cos(0.8*x+log10(y)))/x*1/tan(2*y+z)<<endl<<"b = "<<abs(pow(tan(pow(log(sin(pow(x, 2)+pow(y, 3)+z)), 2)), 3))<<endl;
}
